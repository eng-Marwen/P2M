#these files can't be inside the image at all
node_modules
build
dist
.git
.gitignore
# .env is NOT ignored - it's needed during Docker build for Vite to embed VITE_* variables
# The .env file is only used during build stage and won't exist in the final nginx image
.env.local