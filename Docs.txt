frontend:
-we create pages
-set up the routes (react router dom):
    -// Routes define the structure
    -// useNavigate for programmatic navigation(     // Do some logic, then navigate)
    -Use Link for simple navigation (Recommended for Header)


***tailwind***
-sizes:w-24 sm:w-64 =>the first is for the mobile the second is for the normal pc size
-